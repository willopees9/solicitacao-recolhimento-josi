/** @type {import('next').NextConfig} */
const nextConfig = {
  // Modo estrito do React ajuda a pegar efeitos colaterais indevidos cedo no desenvolvimento.
  reactStrictMode: true,
};

module.exports = nextConfig;
